[package]
name = "core-lib"
version = "0.1.0"
edition = "2021"

[dependencies]
shared = { path = "../shared" }
postcard = { version = "1.0", features = ["use-std"] }
tokio = { version = "1", features = ["full"] }
log = "0.4"
serialport = "4.2"